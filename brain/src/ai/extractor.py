"""Placeholder for structured extraction of patient responses."""


def extract_explicit_facts(response: str) -> dict[str, str]:
    """Keep raw text until a validated extraction provider is configured."""
    return {"raw_response": response.strip()}
